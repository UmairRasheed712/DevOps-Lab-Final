apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: devops-final
stringData:
  # Set at apply-time via kubectl/ansible, do not commit real secrets
  POSTGRES_PASSWORD: "CHANGE_ME"
  SECRET_KEY: "CHANGE_ME"
